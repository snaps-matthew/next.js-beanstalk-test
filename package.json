{
  "name": "sample-next",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "npm run clean && next build",
    "lint": "next lint",
    "clean": "rm -rf .next",
    "deploy-dev": "npm install && npm run build && eb deploy test-next"
  },
  "dependencies": {
    "next": "^12.0.8",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "devDependencies": {
    "eslint": "8.7.0",
    "eslint-config-next": "12.0.8"
  }
}
